{% extends "base.html.twig" %}

{% block body %}
    {% for post in posts %}
        <article class="page page-{{ post.slug }}">
            <h1>{{ post.title }}</h1>
            <div>{{ post.content }}</div>
        </article>
    {% endfor %}
{% endblock %}